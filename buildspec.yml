version: 0.2

phases:
  install:
    commands:
      - echo "nothing to install for static HTML"
  build:
    commands:
      - echo "Zipping files..."
      - echo "zipped to app.zip"
      - zip -r app.zip index.html appspec.yml scripts/
artifacts:
  files:
    - app.zip